<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Параметры наложения ФИО на сертификат (не именной)
    | Все доли — от 0 до 1 от размера изображения (ширина/высота).
    |--------------------------------------------------------------------------
    */

    'certificate' => [

        /** Горизонтальный центр всех трёх текстовых блоков (совпадает с центром фразы «Настоящий сертификат...»). Доля от ширины: 0 = левый край, 1 = правый, 0.69 = правее центра. */
        'center_x' => (float) env('CERT_CENTER_X', 0.73),

        /** Базовая линия первой строки (фамилия). Доля от высоты изображения. */
        'block_top' => (float) env('CERT_BLOCK_TOP', 0.18),

        /** Расстояние между базовыми линиями строк (фамилия → имя → отчество). Доля от высоты изображения. */
        'line_spacing' => (float) env('CERT_LINE_SPACING', 0.05),

        /** Размер шрифта в пунктах (например 24, 28, 32). */
        'font_size' => (int) env('CERT_FONT_SIZE', 50),

        /** Путь к TTF-шрифту с кириллицей. Пусто — авто-поиск (Arial, DejaVu и т.д.). */
        'certificate_font_path' => env('CERT_FONT_PATH'),
    ],
];
