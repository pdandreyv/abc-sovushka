<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Параметры наложения ФИО на сертификат (не именной)
    | Две строки: 1 — фамилия, 2 — имя и отчество в одну строку.
    | Все доли — от 0 до 1 от размера изображения (ширина/высота).
    |--------------------------------------------------------------------------
    */

    'certificate' => [

        /** Горизонтальный центр текстовых блоков. Доля от ширины: 0 = левый край, 1 = правый. */
        'center_x' => (float) env('CERT_CENTER_X', 0.71),

        /** Базовая линия первой строки (фамилия). Доля от высоты изображения. */
        'block_top' => (float) env('CERT_BLOCK_TOP', 0.4),

        /** Расстояние между базовыми линиями двух строк (фамилия → имя отчество). Доля от высоты изображения. */
        'line_spacing' => (float) env('CERT_LINE_SPACING', 0.045),

        /** Размер шрифта в пунктах (например 44, 50, 56). */
        'font_size' => (int) env('CERT_FONT_SIZE', 56),

        /**
         * Путь к TTF-шрифту с кириллицей (например Lumberjack Bold).
         * Пусто — авто-поиск: public/fonts/LumberjackBold.ttf, Lumberjack.ttf, DejaVuSans и т.д.
         * Чтобы использовать Lumberjack: скачайте TTF (Lumberjack Regular/Bold), положите в public/fonts/
         * как LumberjackBold.ttf или укажите полный путь в .env: CERT_FONT_PATH=/path/to/LumberjackBold.ttf
         */
        'certificate_font_path' => env('CERT_FONT_PATH'),
    ],
];
